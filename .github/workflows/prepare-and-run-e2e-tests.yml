name: Prepare and run e2e tests

on:
  workflow_call:
    # inputs:
    #   shell:
    #     description: 'Shell Application version'
    #     default: 'latest'
    #     required: false
    #     type: string  

jobs:
  prepare-and-run:
    name: Prepare and run e2e tests
    runs-on: ubuntu-latest
    steps:
      - name: Prepare e2e environment
        uses: ./.github/workflows/prepare-e2e-tests-env.yml # TODO: Fix link
      - name: Run e2e tests
        uses: ./.github/workflows/run-e2e-tests.yml
